#include<iostream>
using namespace std;
int main(){

    int a = 6, b = 10;

    //or
    cout<<"OR operation "<<(a | b) << endl;

    // and
    cout<<"AND opration "<<(a & b) << endl;

    // XOR(^)
    cout<<"XOR operation "<<(a ^ b) <<endl<<endl;
    return 0;
}